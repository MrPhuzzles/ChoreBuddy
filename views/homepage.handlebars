
{{#each post}}
<div class="row mb-4 blog" >
  <div class="col-md-5" >
    <h2>
      <a href="/post/{{id}}">{{title}}</a>
    </h2>
  
    <p>Created by {{user.username}} on {{created_at}}</p> 
  </div>
  <div class="col-md-7">
    <p>
      {{content}}
    </p>
  </div>
</div>
{{/each}}
